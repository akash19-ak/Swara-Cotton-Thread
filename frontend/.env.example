# Base URL of the deployed backend API (Render), no trailing slash.
# Local dev falls back to http://localhost:5000 automatically if this is unset.
VITE_API_BASE_URL=https://your-backend.onrender.com
